const assert = require('assert');

// Describe tests
describe ('some demo test', function () {
    
    // Create tests
    it ('add two numbers', function () {
        assert (2 + 3 === 5);
    })

})